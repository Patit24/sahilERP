/**
 * DEPRECATED — this file is a stub kept to avoid breaking any stale imports.
 * Firebase configuration is now in src/lib/firebase-client.ts.
 */
export const isLocalCacheDisabled = false
export const supabase = null
